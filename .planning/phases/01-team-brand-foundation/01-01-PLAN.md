# Plan: Phase 01-01 - Data & Team Foundation

Establishing the core data structure and the premium team identity on the About page.

<objective>
Migrate hardcoded project and team data to a centralized source of truth and implement a high-end interactive team list on the About page.
</objective>

<tasks>
  <task type="execute">
    <files>
      <file>lib/data.ts</file>
    </files>
    <read_first>
      <file>components/HorizontalWork.tsx</file>
      <file>components/Team.tsx</file>
    </read_first>
    <action>
      Create `lib/data.ts` and define the `TeamMember` and `Project` interfaces. 
      Populate with the three team members (Devanshu, Jitendrakumar, Rupesh) and the three frontend categories (Creative Development, Next.js & Web Apps, UI/UX & Design Systems) using concept project names.
    </action>
    <verify>
      `ls lib/data.ts` returns 0.
      `grep "Devanshu Shrivastav" lib/data.ts` returns 0.
    </verify>
    <acceptance_criteria>
      - File `lib/data.ts` exists.
      - Includes `team` array with 3 members and their superpowers.
      - Includes `projects` array with concept projects for all 3 frontend categories.
    </acceptance_criteria>
  </task>

  <task type="execute">
    <files>
      <file>components/Team.tsx</file>
    </files>
    <read_first>
      <file>lib/data.ts</file>
    </read_first>
    <action>
      Refactor `components/Team.tsx` to use the data from `lib/data.ts`.
      Implement an interactive typography reveal for the team list.
      Hovering over a member's name should trigger a GSAP animation that reveals their "Superpower" text.
      Ensure the styling is minimalist and premium (no Tegaki/handwriting).
    </action>
    <verify>
      `grep "import { team } from \"@/lib/data\"" components/Team.tsx` returns 0.
    </verify>
    <acceptance_criteria>
      - `components/Team.tsx` uses imported data.
      - Interactive hover effect reveals superpower text.
      - No handwriting fonts used.
    </acceptance_criteria>
  </task>

  <task type="execute">
    <files>
      <file>components/HorizontalWork.tsx</file>
    </files>
    <read_first>
      <file>lib/data.ts</file>
    </read_first>
    <action>
      Refactor `components/HorizontalWork.tsx` to import `projects` from `lib/data.ts`.
      Remove the local `projects` constant.
    </action>
    <verify>
      `grep "import { projects } from \"@/lib/data\"" components/HorizontalWork.tsx` returns 0.
    </verify>
    <acceptance_criteria>
      - `components/HorizontalWork.tsx` uses centralized data.
      - No local `projects` array exists in the file.
    </acceptance_criteria>
  </task>
</tasks>

<verification>
1. Run `yarn dev` and verify the About page shows the new team members.
2. Verify hovering over a team member reveals their superpower with a smooth GSAP animation.
3. Verify the Work section on the home page shows the new concept project categories.
</verification>

<success_criteria>
- Project data is centralized in `lib/data.ts`.
- About page team list is interactive and premium.
- All team members (Devanshu, Jitendrakumar, Rupesh) are correctly represented.
- Work categories reflect "Frontend Technology" focus.
</success_criteria>

<must_haves>
- [ ] Centralized data structure in `lib/data.ts`.
- [ ] Interactive hover reveal in `Team.tsx`.
- [ ] Correct team names and roles on the About page.
</must_haves>
