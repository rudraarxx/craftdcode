# Plan: Phase 02-01 - Homepage Refinement

Elevating the homepage experience with dynamic architectural masking and atmospheric mouse-following glows.

<objective>
Implement a mouse-following atmospheric aura and a dynamic, deconstructed masking effect for the Hero title.
</objective>

<tasks>
  <task type="execute">
    <files>
      <file>components/Aura.tsx</file>
    </files>
    <action>
      Create a new `Aura` component. 
      Use GSAP `quickTo` to make a soft radial-gradient glow (low opacity, high blur) follow the mouse cursor.
      Ensure it is positioned fixed in the background with `z-index: -1`.
    </action>
    <verify>
      `ls components/Aura.tsx` returns 0.
    </verify>
    <acceptance_criteria>
      - Component exists.
      - Smooth mouse follow animation.
      - "Grounded Ethereal" aesthetic (soft, subtle).
    </acceptance_criteria>
  </task>

  <task type="execute">
    <files>
      <file>app/layout.tsx</file>
    </files>
    <read_first>
      <file>components/Aura.tsx</file>
    </read_first>
    <action>
      Import and add the `Aura` component to the root layout so it is present across the homepage.
    </action>
    <verify>
      `grep "<Aura />" app/layout.tsx` returns 0.
    </verify>
    <acceptance_criteria>
      - Aura is globally available.
    </acceptance_criteria>
  </task>

  <task type="execute">
    <files>
      <file>components/Hero.tsx</file>
    </files>
    <action>
      Refactor the Hero component to implement "Architectural Cuts".
      Duplicate the "CRAFT DCODE" title into multiple layers.
      Apply `clip-path` polygons to create deconstructed pieces.
      Animate these pieces with GSAP (scroll-responsive or drifting) to create a premium, high-end feel.
      Ensure accessibility by keeping one layer visible to screen readers.
    </action>
    <verify>
      `grep "clipPath" components/Hero.tsx` returns 0.
    </verify>
    <acceptance_criteria>
      - Dynamic masking implemented.
      - Shifting/cutting effect is visible and smooth.
      - High-contrast, premium aesthetic maintained.
    </acceptance_criteria>
  </task>
</tasks>

<verification>
1. Open the homepage and verify the mouse-following glow is smooth and subtle.
2. Verify the Hero title has the "cut" effect and animates slightly (scroll or drift).
3. Ensure no layout shifts or interaction blocking occurs.
</verification>

<success_criteria>
- Homepage feels "alive" and interactive.
- Hero title is architecturally deconstructed.
- Performance remains high (smooth scrolling).
</success_criteria>

<must_haves>
- [ ] Mouse-following Aura component.
- [ ] Dynamic clip-path masking on Hero title.
- [ ] Z-index management to ensure aura is in the background.
</must_haves>
