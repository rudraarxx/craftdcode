# Plan: Phase 02-02 - Premium Case Studies

Implementing dynamic routing and building the process-oriented case study template.

<objective>
Fix the routing structure, expand the project data, and implement the premium case study template with slug-based navigation.
</objective>

<tasks>
  <task type="execute">
    <files>
      <file>app/work/[slug]/page.tsx</file>
    </files>
    <action>
      Rename the incorrectly named directory `app/work/%5Bslug%5D` to `app/work/[slug]`.
      If the rename fails, move the file manually.
    </action>
    <verify>
      `ls app/work/[slug]/page.tsx` returns 0.
    </verify>
    <acceptance_criteria>
      - Standard Next.js dynamic routing directory `[slug]` exists.
    </acceptance_criteria>
  </task>

  <task type="execute">
    <files>
      <file>lib/data.ts</file>
    </files>
    <action>
      Expand the `Project` interface to include `slug`, `role`, `challenge`, `solution`, and `process` fields.
      Update the existing concept projects with descriptive slugs (e.g., `aura-immersive-experience`) and detailed content for each section.
    </action>
    <verify>
      `grep "slug:" lib/data.ts` returns 0.
    </verify>
    <acceptance_criteria>
      - Data schema is expanded.
      - Slugs are added to all projects.
    </acceptance_criteria>
  </task>

  <task type="execute">
    <files>
      <file>app/work/[slug]/page.tsx</file>
    </files>
    <read_first>
      <file>lib/data.ts</file>
    </read_first>
    <action>
      Refactor the case study page to fetch data from `lib/data.ts` based on the `slug` param.
      Update the layout to be more "Process-oriented":
      - Premium Hero with parallax.
      - Challenge/Solution section.
      - **New**: Technical/Process section (displaying tech stack or performance metrics).
      - Polished "Back to Work" button.
    </action>
    <verify>
      `grep "projects.find" app/work/[slug]/page.tsx` returns 0.
    </verify>
    <acceptance_criteria>
      - Page fetches data dynamically.
      - New "Process" section implemented.
      - Premium typography and layout maintained.
    </acceptance_criteria>
  </task>

  <task type="execute">
    <files>
      <file>components/HorizontalWork.tsx</file>
    </files>
    <action>
      Update the `Link` components to use the new `slug` property instead of the `id`.
    </action>
    <verify>
      `grep "project.slug" components/HorizontalWork.tsx` returns 0.
    </verify>
    <acceptance_criteria>
      - Homepage links to slugs.
    </acceptance_criteria>
  </task>
</tasks>

<verification>
1. Click a project on the homepage and verify it redirects to `/work/aura-immersive-experience` (slug).
2. Verify the detail page shows the correct data for that project.
3. Verify the "Process" section is visible and styled.
4. Test the "Back to all work" button.
</verification>

<success_criteria>
- Clean, SEO-friendly routing implemented.
- Case studies feel deep and technical.
- Dynamic data pipeline is fully functional.
</success_criteria>

<must_haves>
- [ ] Correct [slug] directory structure.
- [ ] Slug-based links in `HorizontalWork.tsx`.
- [ ] Expanded data in `lib/data.ts`.
- [ ] Technical/Process section on detail pages.
</must_haves>
