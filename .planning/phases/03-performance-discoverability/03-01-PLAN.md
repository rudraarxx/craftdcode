# Plan: Phase 03-01 - Performance & Discoverability

Implementing SEO infrastructure, brand assets, and performing a semantic audit for launch readiness.

<objective>
Configure Next.js metadata, sitemaps, and brand assets to ensure the site is fast, shareable, and search-engine optimized.
</objective>

<tasks>
  <task type="execute">
    <files>
      <file>app/icon.svg</file>
      <file>app/opengraph-image.tsx</file>
    </files>
    <read_first>
      <file>public/logo.svg</file>
    </read_first>
    <action>
      1. Copy `public/logo.svg` to `app/icon.svg` to serve as the site's favicon.
      2. Create `app/opengraph-image.tsx` using the `ImageResponse` API. Use the `logo.svg` to create a premium-looking social share card.
    </action>
    <verify>
      `ls app/icon.svg` returns 0.
    </verify>
    <acceptance_criteria>
      - Favicon is active.
      - Social card generator exists.
    </acceptance_criteria>
  </task>

  <task type="execute">
    <files>
      <file>app/sitemap.ts</file>
      <file>app/robots.ts</file>
    </files>
    <read_first>
      <file>lib/data.ts</file>
    </read_first>
    <action>
      1. Create `app/sitemap.ts` to generate a dynamic sitemap including home, about, and all project slugs from `lib/data.ts`.
      2. Create `app/robots.ts` to allow all search engines.
    </action>
    <verify>
      `ls app/sitemap.ts` returns 0.
    </verify>
    <acceptance_criteria>
      - Sitemap contains all project URLs.
      - Robots.txt is properly configured.
    </acceptance_criteria>
  </task>

  <task type="execute">
    <files>
      <file>app/work/[slug]/page.tsx</file>
    </files>
    <action>
      Implement `export async function generateMetadata({ params })` in the case study page.
      Fetch the project data and set the `title` and `description` dynamically for SEO.
    </action>
    <verify>
      `grep "generateMetadata" app/work/[slug]/page.tsx` returns 0.
    </verify>
    <acceptance_criteria>
      - Case studies show unique titles in browser tabs and social cards.
    </acceptance_criteria>
  </task>

  <task type="execute">
    <files>
      <file>app/layout.tsx</file>
      <file>components/Header.tsx</file>
      <file>components/Footer.tsx</file>
    </files>
    <action>
      Perform a semantic audit:
      - Update `app/layout.tsx` with enhanced global metadata (keywords, OpenGraph types).
      - Ensure `Header` uses `<header>`, `Footer` uses `<footer>`, and main content is wrapped in `<main>`.
      - Check heading hierarchy (H1-H3).
    </action>
    <verify>
      `grep "<header" components/Header.tsx` returns 0.
    </verify>
    <acceptance_criteria>
      - Clean semantic HTML structure.
      - Enhanced global metadata.
    </acceptance_criteria>
  </task>
</tasks>

<verification>
1. Check `/sitemap.xml` and `/robots.txt` in the browser (dev server).
2. Use a social card validator or check the meta tags in the head for project pages.
3. Verify the favicon appears in the browser tab.
4. Check the console for any "Main-thread blocking" warnings during animations.
</verification>

<success_criteria>
- Site is fully indexable by search engines.
- Social sharing looks premium with brand-consistent images.
- HTML structure is semantically correct.
- Metadata is dynamic and descriptive.
</success_criteria>

<must_haves>
- [ ] Active favicon from `logo.svg`.
- [ ] Dynamic sitemap with all project slugs.
- [ ] Correct semantic tags on all core layout components.
- [ ] Dynamic metadata for case study pages.
</must_haves>
