# Plan: Phase 04-01 - Sanity Infrastructure & Schemas

Setting up the content management engine and defining the dashboard architecture.

<objective>
Install Sanity, configure the Studio, and define schemas for Projects, Team, Blog, Testimonials, and Services.
</objective>

<tasks>
  <task type="execute">
    <files>
      <file>package.json</file>
    </files>
    <action>
      Install the required Sanity packages: `sanity`, `next-sanity`, `@sanity/image-url`, `@sanity/vision`, and `styled-components`.
    </action>
    <verify>
      `grep "next-sanity" package.json` returns 0.
    </verify>
    <acceptance_criteria>
      - Dependencies installed.
    </acceptance_criteria>
  </task>

  <task type="execute">
    <files>
      <file>sanity.config.ts</file>
      <file>sanity.cli.ts</file>
    </files>
    <action>
      Create the Sanity configuration files using Project ID `utbonnz3` and dataset `production`.
      Define the base path as `/studio`.
    </action>
    <verify>
      `ls sanity.config.ts` returns 0.
    </verify>
    <acceptance_criteria>
      - Config files exist and use the correct Project ID.
    </acceptance_criteria>
  </task>

  <task type="execute">
    <files>
      <file>sanity/schemaTypes/index.ts</file>
      <file>sanity/schemaTypes/project.ts</file>
      <file>sanity/schemaTypes/teamMember.ts</file>
      <file>sanity/schemaTypes/post.ts</file>
      <file>sanity/schemaTypes/testimonial.ts</file>
      <file>sanity/schemaTypes/service.ts</file>
    </files>
    <action>
      Define the 5 core schemas:
      - `project`: Rich case study fields.
      - `teamMember`: Team roles and superpowers.
      - `post`: Full blog capability (Portable Text).
      - `testimonial`: Social proof.
      - `service`: Studio offerings.
    </action>
    <verify>
      `ls sanity/schemaTypes/project.ts` returns 0.
    </verify>
    <acceptance_criteria>
      - All 5 schemas are defined and exported.
    </acceptance_criteria>
  </task>

  <task type="execute">
    <files>
      <file>app/studio/[[...index]]/page.tsx</file>
      <file>app/studio/[[...index]]/layout.tsx</file>
    </files>
    <action>
      Create the catch-all route to mount the Sanity Studio inside the Next.js app.
      Ensure the layout is clean (no global headers/footers) for the Studio UI.
    </action>
    <verify>
      `ls app/studio/[[...index]]/page.tsx` returns 0.
    </verify>
    <acceptance_criteria>
      - Studio is accessible at `/studio`.
    </acceptance_criteria>
  </task>
</tasks>

<verification>
1. Run the dev server and navigate to `/studio`.
2. Verify the login screen appears.
3. Once logged in (user manual action), verify all 5 content types are visible in the sidebar.
</verification>

<success_criteria>
- Sanity Studio is fully functional within the app.
- Schemas are architecturally sound and cover all project requirements.
- Dashboard is accessible for content management.
</success_criteria>

<must_haves>
- [ ] Sanity Studio at `/studio`.
- [ ] Project ID `utbonnz3` configured.
- [ ] Schemas for Projects, Team, Blog, Testimonials, and Services.
</must_haves>
