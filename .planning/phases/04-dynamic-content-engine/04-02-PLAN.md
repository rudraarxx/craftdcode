# Plan: Phase 04-02 - Data Integration & Migration

Connecting the Next.js frontend to Sanity and transitioning the data layer.

<objective>
Refactor the site to fetch content from Sanity and provide utility for data migration.
</objective>

<tasks>
  <task type="execute">
    <files>
      <file>lib/sanity.client.ts</file>
      <file>lib/sanity.queries.ts</file>
    </files>
    <action>
      Create the Sanity client and define GROQ queries for projects, team members, testimonials, and services.
    </action>
    <verify>
      `ls lib/sanity.client.ts` returns 0.
    </verify>
    <acceptance_criteria>
      - Client and queries exist.
    </acceptance_criteria>
  </task>

  <task type="execute">
    <files>
      <file>app/page.tsx</file>
      <file>components/Team.tsx</file>
      <file>components/HorizontalWork.tsx</file>
    </files>
    <action>
      Refactor the homepage and core components to fetch data from Sanity.
      Replace `lib/data.ts` imports with Sanity client calls.
    </action>
    <verify>
      `grep "client.fetch" components/Team.tsx` returns 0.
    </verify>
    <acceptance_criteria>
      - Homepage data is dynamic.
    </acceptance_criteria>
  </task>

  <task type="execute">
    <files>
      <file>app/work/[slug]/page.tsx</file>
    </files>
    <action>
      Refactor the case study detail page to fetch data by slug from Sanity.
      Ensure images are handled via `@sanity/image-url`.
    </action>
    <verify>
      `grep "urlFor" app/work/[slug]/page.tsx` returns 0.
    </verify>
    <acceptance_criteria>
      - Project pages fetch data from Sanity.
    </acceptance_criteria>
  </task>

  <task type="execute">
    <files>
      <file>lib/data.ts</file>
    </files>
    <action>
      Deprecate `lib/data.ts` by removing its contents once migration is confirmed (or keep as a fallback).
    </action>
    <verify>
      `cat lib/data.ts` shows deprecation notice.
    </verify>
    <acceptance_criteria>
      - Static data layer is removed.
    </acceptance_criteria>
  </task>
</tasks>

<verification>
1. Add a "Test Project" in Sanity Studio at `/studio`.
2. Verify it appears on the homepage instantly.
3. Verify the project detail page renders all rich content fields correctly.
</verification>

<success_criteria>
- Frontend is fully decoupled from static code.
- Data fetching is optimized and performant.
- All new content types (Testimonials, Services) are ready for UI implementation.
</success_criteria>

<must_haves>
- [ ] Sanity Client integration.
- [ ] GROQ queries for all content types.
- [ ] Refactored frontend components.
</must_haves>
